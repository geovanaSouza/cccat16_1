{
  "name": "ride",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@types/express": "^4.17.17",
    "@types/jest": "^29.5.4",
    "axios": "^1.5.0",
    "express": "^4.18.2",
    "jest": "^29.6.4",
    "nodemon": "^3.0.1",
    "pg-promise": "^11.5.4",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "pretest": "docker-compose up -d --build",
    "test": "jest",
    "posttest": "docker-compose stop && docker-compose rm --force",
    "build": "tsc",
    "start": "docker-compose up -d --build",
    "stop": "docker-compose stop && docker-compose rm --force"
  }
}
